import re

# # randStr = "cat cats doctors doctor's"

# randStr = "doctor doctors doctor's"

# # regex = re.compile("[cat]+s?")

# regex = re.compile("[doctor]+['s]*")

# matches = re.findall(regex, randStr)

# for i in matches:
#     print(i)

### problems 
